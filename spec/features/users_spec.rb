require 'rails_helper'

let(:admin)         { build(:role , role_type:1)}
let(:product_owner) { build(:role , role_type:2)}
let(:reporter)      { build(:role , role_type:3)}

before


describe 'Create users' do

end
